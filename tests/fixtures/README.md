# Fixtures Overview

`tests/fixtures/` 按业务模块拆分，后续新增测试输入文件时优先放到对应模块目录下。

- `auth/`
  登录相关静态输入与示例数据。
- `review/`
  智能审查主流程相关输入文件。
- `checklist/`
  审查清单模块相关数据。
- `rule/`
  审查规则模块相关数据。
- `import/`
  导入清单模块相关夹具与历史样例。
